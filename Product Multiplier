<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>The Product Multiplier Playbook — nothing but static</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg: #0b0d10;          /* page background */
      --panel: #0f1216;       /* canvas panel */
      --ink: #e6ecf2;         /* primary text */
      --muted: #98a2b3;       /* secondary text */
      --line: rgba(255,255,255,.08);
      --accent: #7c5cff;      /* neon violet */
      --accent-2:#00ffc6;     /* mint */
      --accent-3:#ff2d55;     /* punch */
      --ring: rgba(124,92,255,.42);
      --shadow: 0 12px 50px rgba(0,0,0,.55);
      --radius: 28px;
      --orbit: 340px;         /* distance from center to cards */
      --card-w: 290px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; color:var(--ink); background:
        radial-gradient(900px 700px at 18% 8%, #111624, #0a0d13 55%, #07090d 100%),
        var(--bg);
      font-family: Inter, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
      letter-spacing:.01em;
    }
    .wrap{min-height:100svh; display:grid; place-items:center; padding:28px}

    /* --- Canvas --- */
    .canvas{
      width: min(1200px, 96vw);
      aspect-ratio: 16 / 9;
      background:
        linear-gradient(180deg, rgba(124,92,255,.06), rgba(0,255,198,.06) 60%, rgba(255,45,85,.06)),
        var(--panel);
      border:1px solid var(--line);
      border-radius: var(--radius);
      position:relative; overflow:hidden; box-shadow: var(--shadow);
      isolation:isolate; /* so filters don’t bleed */
    }
    /* subtle tech grid */
    .canvas::before{
      content:""; position:absolute; inset:0; pointer-events:none; opacity:.2; mix-blend-mode:overlay;
      background:
        repeating-linear-gradient(0deg, transparent 0 28px, rgba(255,255,255,.04) 28px 29px),
        repeating-linear-gradient(90deg, transparent 0 28px, rgba(255,255,255,.04) 28px 29px);
    }
    /* vignette + neon fog */
    .canvas::after{
      content:""; position:absolute; inset:-20%; pointer-events:none; z-index:0;
      background:
        radial-gradient(560px 320px at 20% 85%, rgba(124,92,255,.18), transparent 60%),
        radial-gradient(520px 380px at 85% 20%, rgba(0,255,198,.14), transparent 60%),
        radial-gradient(800px 520px at 50% -10%, rgba(255,45,85,.12), transparent 70%),
        radial-gradient(100% 100% at 50% 50%, rgba(0,0,0,.35), transparent 60%);
      filter: saturate(1.1);
    }

    /* --- Header --- */
    .header{
      position:absolute; inset:24px 28px auto 28px; z-index:5;
      display:flex; align-items:flex-start; justify-content:space-between; gap:16px;
    }
    .title{font-weight:800; font-size: clamp(22px, 2.4vw, 34px); letter-spacing:.02em}
    .subtitle{color:var(--muted); font-weight:600; margin-top:6px; font-size: clamp(12px, 1.2vw, 14px)}

    /* --- Core layout (radial) --- */
    .stage{
      position:absolute; inset:0; display:grid; place-items:center; z-index:1;
      padding:80px; /* breathing room for labels */
    }
    .hub{
      position:relative; z-index:3; text-align:center; width:min(420px, 70%);
      padding:28px 24px; border-radius: 1000px;
      background: linear-gradient(180deg, rgba(124,92,255,.18), rgba(0,255,198,.16));
      border:1px solid rgba(255,255,255,.12);
      box-shadow: inset 0 0 0 1px rgba(255,255,255,.06), 0 10px 30px rgba(0,0,0,.4);
    }
    .hub .kicker{font-family: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace; color: var(--accent-2); font-size:12px; letter-spacing:.14em; text-transform:uppercase; display:block; margin-bottom:10px}
    .hub .big{font-weight:800; font-size: clamp(28px, 3vw, 40px); letter-spacing:.02em}
    .hub .tag{margin-top:8px; color:var(--muted); font-size: 14px}

    /* Decorative orbit ring */
    .orbit{ position:absolute; inset:0; z-index:2; display:grid; place-items:center; pointer-events:none }
    .orbit .ring{ width: calc(var(--orbit) * 2 + 40px); height: calc(var(--orbit) * 2 + 40px); border-radius:50%;
      border:1px dashed rgba(255,255,255,.10); box-shadow: 0 0 80px var(--ring) inset; }

    /* Rays from center */
    .rays{ position:absolute; inset:0; z-index:1; pointer-events:none }
    .ray{ position:absolute; left:50%; top:50%; height:2px; width: calc(var(--orbit) + 40px); transform-origin:0 50%;
      background: linear-gradient(90deg, rgba(124,92,255,.55), rgba(124,92,255,0)); opacity:.7; filter: drop-shadow(0 0 6px rgba(124,92,255,.35)); }
    .ray[data-angle]{ transform: rotate(var(--angle)) translateZ(0); }

    /* Spoke cards */
    .spoke{ position:absolute; left:50%; top:50%; width:var(--card-w); transform:
      rotate(var(--angle)) translate(var(--orbit)) rotate(calc(var(--angle) * -1)); z-index:3; }
    .card{
      background: linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
      border:1px solid rgba(255,255,255,.10);
      border-radius: 18px; padding:16px 16px 14px; backdrop-filter: blur(6px);
      box-shadow: 0 8px 24px rgba(0,0,0,.35), inset 0 0 0 1px rgba(255,255,255,.04);
    }
    .eyebrow{ font-family:"JetBrains Mono", ui-monospace, monospace; font-size:11px; letter-spacing:.18em; text-transform:uppercase; color:var(--accent-2); opacity:.95 }
    .h3{ margin-top:6px; font-weight:800; font-size:18px; letter-spacing:.02em }
    .list{ margin:10px 0 0; padding-left:16px; color:var(--muted); line-height:1.45; font-size:14px }
    .list li{ margin: 6px 0 }

    /* Footer branding */
    .footer{
      position:absolute; inset:auto 24px 20px 24px; display:flex; justify-content:space-between; align-items:center; gap:12px; z-index:5;
      color:var(--muted); font-size:12px
    }
    .brand a{ color:var(--ink); text-decoration:none; border-bottom:1px dotted rgba(255,255,255,.24) }

    /* Print & export helpers */
    @media print{
      body{ background:#fff; -webkit-print-color-adjust: exact; print-color-adjust: exact }
      .canvas{ width: 1200px; aspect-ratio:auto }
    }
  </style>
</head>
<body>
  <div class="wrap">
    <section class="canvas" role="img" aria-label="Infographic: The Product Multiplier Playbook — central hub with five branches for AI Fluency, Data Intuition, Systems Thinking, Cross-Domain Literacy, Storytelling & Experimentation.">
      <!-- Header -->
      <header class="header">
        <div>
          <div class="title">The Product Multiplier Playbook</div>
          <div class="subtitle">From backlog babysitter to outcome amplifier</div>
        </div>
      </header>

      <!-- Radial Stage -->
      <div class="stage">
        <!-- Center Hub -->
        <div class="hub" aria-label="Amplify Outcomes center">
          <span class="kicker">Center</span>
          <div class="big">AMPLIFY OUTCOMES</div>
          <div class="tag">Don’t just manage requirements. Multiply results.</div>
        </div>

        <!-- Orbit Ring -->
        <div class="orbit"><div class="ring" aria-hidden="true"></div></div>

        <!-- Rays (decorative lines from center) -->
        <div class="rays" aria-hidden="true">
          <span class="ray" style="--angle: 0deg"></span>
          <span class="ray" style="--angle: 72deg"></span>
          <span class="ray" style="--angle: 144deg"></span>
          <span class="ray" style="--angle: 216deg"></span>
          <span class="ray" style="--angle: 288deg"></span>
        </div>

        <!-- Spoke Cards -->
        <div class="spoke" style="--angle: 0deg">
          <article class="card" aria-labelledby="ai-fluency">
            <div class="eyebrow">01</div>
            <div class="h3" id="ai-fluency">AI Fluency</div>
            <ul class="list">
              <li>Prompts → Workflows → Harnesses</li>
              <li>Tools as collaborators, not assistants</li>
              <li>Automate research, specs, & reviews</li>
            </ul>
          </article>
        </div>

        <div class="spoke" style="--angle: 72deg">
          <article class="card" aria-labelledby="data-intuition">
            <div class="eyebrow">02</div>
            <div class="h3" id="data-intuition">Data Intuition</div>
            <ul class="list">
              <li>Read signals fast</li>
              <li>Test → Learn → Iterate</li>
              <li>Hypotheses over hunches</li>
            </ul>
          </article>
        </div>

        <div class="spoke" style="--angle: 144deg">
          <article class="card" aria-labelledby="systems-thinking">
            <div class="eyebrow">03</div>
            <div class="h3" id="systems-thinking">Systems Thinking</div>
            <ul class="list">
              <li>Product ↔ Tech ↔ Business</li>
              <li>Map incentives & behaviors</li>
              <li>Design for feedback loops</li>
            </ul>
          </article>
        </div>

        <div class="spoke" style="--angle: 216deg">
          <article class="card" aria-labelledby="cross-domain">
            <div class="eyebrow">04</div>
            <div class="h3" id="cross-domain">Cross‑Domain Literacy</div>
            <ul class="list">
              <li>Import ideas from psych, econ, art</li>
              <li>Innovate at the intersections</li>
              <li>Steal like a system</li>
            </ul>
          </article>
        </div>

        <div class="spoke" style="--angle: 288deg">
          <article class="card" aria-labelledby="story-experiment">
            <div class="eyebrow">05</div>
            <div class="h3" id="story-experiment">Storytelling & Experimentation</div>
            <ul class="list">
              <li>Make the complex feel inevitable</li>
              <li>Prototype boldly, cheaply, often</li>
              <li>Mistakes as multipliers</li>
            </ul>
          </article>
        </div>
      </div>

      <!-- Footer / Branding -->
      <footer class="footer">
        <div class="brand">Designed for <a href="https://nothingbutstatic.substack.com/" target="_blank" rel="noopener">nothing but static</a></div>
        <div class="brand">© 2025</div>
      </footer>
    </section>
  </div>

  <!-- Tiny helper to export as PNG via browser screenshot tools; we’ll wire up a one‑click export in a later pass if needed. -->
</body>
</html>
